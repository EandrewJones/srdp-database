The shell test scripts depend on two command line utilities:
1) HTTPie
2) jq

Both must be installed. On debian linux run the following commands:
```
sudo apt-get upgrade
sudo apt-get install httpie jq
```
